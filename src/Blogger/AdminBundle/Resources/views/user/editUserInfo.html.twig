{% extends 'BloggerAdminBundle::layout.html.twig' %}

{% block body %}
    <h1>Editing {{ user.username }} </h1>
    {{ render(controller('BloggerAdminBundle:User:edit', {'userId': user.id } )) }}
{% endblock %}